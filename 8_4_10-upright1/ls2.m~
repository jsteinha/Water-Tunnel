function v = ls2(A,B)
    % two-sided least squares
    % minimizes ||Ax-By|| subject to ||y|| = 1
    X = [A -B]'*[A -B];
    Y = diag([zeros(1,size(A,2)) ones(1,size(B,2))]);
    [v,d] = eig(X,Y);
    [lambda,ind] = min(d);
    
    
    
end